__pycache__/
*.py[cod]
.venv/
.venv-dev/
venv/

# Model weights -- multi-GB, fetched by scripts, never committed.
models/
*.gguf
*.bin

# Recordings and scratch audio.
*.wav
!tests/fixtures/*.wav

# Local overrides of config/default.yaml.
config/local.yaml
.DS_Store

# whisper.cpp / llama.cpp checkouts — rebuilt on the Pi by scripts, not vendored.
vendor/

# Local Claude Code settings (machine-specific hooks).
.claude/settings.local.json

# Local assistant notes: contains host/user details, stays off GitHub.
CLAUDE.md
